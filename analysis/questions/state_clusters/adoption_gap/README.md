# Adoption Gap

Cluster states by the relative gap (ceiling − confirmed) / confirmed per major sector — measuring where AI has the most room to expand beyond current confirmed usage.

Run: `venv/Scripts/python -m analysis.questions.state_clusters.adoption_gap.run`
