# Agentic Profile

Cluster states by the ratio of agentic (AEI API) workers to confirmed (all confirmed) workers within each major sector — measuring how "agentic-leaning" each sector is in each state's workforce mix.

Run: `venv/Scripts/python -m analysis.questions.state_clusters.agentic_profile.run`
